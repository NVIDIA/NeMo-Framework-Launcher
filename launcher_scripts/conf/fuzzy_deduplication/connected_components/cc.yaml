run:
  name: 'connected_component'
  results_dir: ${base_results_dir}/${.name}
  time_limit: "08:00:00"
  dependency: "singleton"
  nodes: 2
  partition:

dask:
  pool_size: 72GiB
  protocol: ucx
  interface: ibp12s0

jaccard_pairs_path: 
output_dir: 
cache_dir: 
