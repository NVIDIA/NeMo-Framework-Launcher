run:
  name: 'fuzzy-deduplication'
  results_dir: ${base_results_dir}/${.name}

defaults:
  - compute_minhashes: compute_minhashes
  - minhash_buckets: minhash_buckets
  - jaccard_map_buckets: jaccard_map_buckets
  - jaccard_shuffle: jaccard_shuffle
  - jaccard_compute: jaccard_compute
  - connected_component: connected_component
  - write_deduped_result_with_text: write_deduped_result_with_text
  - verify_all_pairs_jaccard: verify_all_pairs_jaccard
