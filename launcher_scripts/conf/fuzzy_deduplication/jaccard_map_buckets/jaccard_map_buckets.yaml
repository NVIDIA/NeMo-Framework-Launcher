run:
  name: 'jaccard-map-buckets'
  results_dir: ${base_results_dir}/${.name}
  time_limit: "02:00:00"
  dependency: "singleton"
  nodes: 2
  partition: 

dask:
  pool_size: 72GiB
  protocol: ucx
  interface: ibp12s0

input_bucket_dir: ${fuzzy_deduplication.run.results_dir}/buckets.parquet 
num_files: -1
text_ddf_blocksize: 512
input_data_dirs: 
output_dir: ${fuzzy_deduplication.run.results_dir}
