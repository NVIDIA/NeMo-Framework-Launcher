run:
  name: 'minhash-buckets'
  results_dir: ${base_results_dir}/${.name}
  time_limit: "08:00:00"
  dependency: "singleton"
  nodes: 2
  partition:

dask:
  pool_size: 72GiB
  protocol: ucx
  interface: ibp12s0

input_data_dirs: 
minhash_length: 260
num_bands: 20
buckets_per_shuffle: 
