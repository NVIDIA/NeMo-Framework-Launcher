run:
  name: 'verify-all-pairs-jaccard'
  results_dir: ${base_results_dir}/${.name}
  time_limit: "08:00:00"
  dependency: "singleton"
  nodes:
  partition:

dask:
  pool_size: 72GiB
  protocol: ucx
  interface: ibp12s0

