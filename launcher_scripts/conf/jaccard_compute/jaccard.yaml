run:
  name: 'jaccard-compute'
  results_dir: ${base_results_dir}/${.name}
  time_limit: "08:00:00"
  dependency: "singleton"
  nodes: 2
  partition:

dask:
  pool_size: 72GiB
  protocol: ucx
  interface: ibp12s0

shuffled_docs_path: 
output_dir: 
files_per_partition: 5
num_files: -1
log_frequency: 250
