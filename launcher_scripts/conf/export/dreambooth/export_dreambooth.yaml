run:
  name: export_${.model_train_name}
  time_limit: "2:00:00"
  model_train_name: "dreambooth"
  dependency: "singleton"
  training_dir: ${base_results_dir}/${.model_train_name}
  config_summary: dreambooth_fp${export.model.precision}
  results_dir: ${base_results_dir}/${.model_train_name}/export_${.config_summary}
  model_type: "dreambooth"

model:
  restore_from_path: ${base_results_dir}/${export.run.model_train_name}/results/checkpoints/nemo_dreambooth.nemo  # Path to a trained CLIP .nemo file
  precision: 16  # 32|16

infer:
  unconditional_guidance_scale: 7.5
  num_images_per_prompt: 4
  height: 512
  width: 512
  down_factor: 8
  inference_steps: 100
  sampler_type: 'DDIM'
  eta: 0
  out_path: ${export.run.results_dir}
