run:
  name: 'redis-cluster'
  results_dir: ${exact_deduplication.run.results_dir}/${.name}
  time_limit: "1-00:00:00"
  dependency: "singleton"
  nodes: 3
  partition: cpu_long_m2
  cpus_per_node: 48
